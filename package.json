{
  "name": "northwood-game-haven",
  "version": "1.0.0",
  "description": "Northwood Game Haven — marketing site + play-space booking application",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.1",
    "@netlify/neon": "^0.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
